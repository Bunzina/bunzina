{
  "annotations": { "list": [] },
  "editable": false,
  "graphTooltip": 0,
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "reqps" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "title": "Requests per second",
      "type": "timeseries",
      "targets": [
        {
          "expr": "sum by (status_code) (rate(bunzina_http_requests_total{job=\"bunzina\"}[5m]))",
          "legendFormat": "{{status_code}}",
          "refId": "A"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "s" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "title": "P95 request latency",
      "type": "timeseries",
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum by (le) (rate(bunzina_http_request_duration_seconds_bucket{job=\"bunzina\"}[5m])))",
          "legendFormat": "p95",
          "refId": "A"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 },
      "id": 3,
      "title": "Requests in flight",
      "type": "timeseries",
      "targets": [
        {
          "expr": "bunzina_http_requests_in_flight{job=\"bunzina\"}",
          "legendFormat": "in flight",
          "refId": "A"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "reqps" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 },
      "id": 4,
      "title": "Authentication attempts",
      "type": "timeseries",
      "targets": [
        {
          "expr": "sum by (result) (rate(bunzina_authentication_attempts_total{job=\"bunzina\"}[5m]))",
          "legendFormat": "{{result}}",
          "refId": "A"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "reqps" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 16 },
      "id": 5,
      "title": "Notifications",
      "type": "timeseries",
      "targets": [
        {
          "expr": "sum by (channel, result) (rate(bunzina_notifications_total{job=\"bunzina\"}[5m]))",
          "legendFormat": "{{channel}} / {{result}}",
          "refId": "A"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "reqps" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 16 },
      "id": 6,
      "title": "Service order events",
      "type": "timeseries",
      "targets": [
        {
          "expr": "sum by (event, status) (rate(bunzina_service_orders_total{job=\"bunzina\"}[5m]))",
          "legendFormat": "{{event}} / {{status}}",
          "refId": "A"
        }
      ]
    }
  ],
  "schemaVersion": 41,
  "tags": ["bunzina", "api"],
  "templating": { "list": [] },
  "time": { "from": "now-1h", "to": "now" },
  "title": "Bunzina API",
  "uid": "bunzina-api",
  "version": 1
}
