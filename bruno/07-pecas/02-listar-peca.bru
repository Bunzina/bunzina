meta {
  name: Listar peca
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/auto-parts?page={{page}}&limit={{limit}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

params:query {
  page: {{page}}
  limit: {{limit}}
  ~name: Filtro
}
